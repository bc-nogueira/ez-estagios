<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="#">EzEstágio</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to root_path, class: 'nav-link' do %>
          <%= fa_icon('home') %> Home
        <% end %>
      </li>
      <% if account_signed_in? %>
        <li class="nav-item">
          <%= link_to instrucao_index_path, class: 'nav-link' do %>
            <%= fa_icon('info') %> Instruções
          <% end %>
        </li>
        <% if current_account.is_empresa? %>
          <%= render 'layouts/links_empresa' %>
        <% end %>
        <% if current_account.is_coordenador? %>
          <%= render 'layouts/links_coordenador' %>
        <% end %>
          <% if current_account.is_aluno? %>
          <%= render 'layouts/links_aluno' %>
          <% end %>
      <% end %>
    </ul>
    <% if account_signed_in? %>
      <span class="navbar-text text-white mr-3">
        Olá <b><%= current_account.perfil.nome %></b>!
      </span>

      <%= link_to 'Sair', destroy_account_session_path, method: :delete, class: 'btn btn-outline-light my-2 my-sm-0' %>
    <% else %>
      <%= link_to 'Entrar', new_account_session_path, class: 'btn btn-outline-light my-2 my-sm-0 mr-3' %>
      <%= link_to 'Cadastrar', new_account_registration_path, class: 'btn btn-outline-light my-2 my-sm-0' %>
    <% end %>
  </div>
</nav>