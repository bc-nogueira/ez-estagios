<%= form_with(model: vaga_habilidade, local: true) do |form| %>
  <% if vaga_habilidade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vaga_habilidade.errors.count, "error") %> prohibited this vaga_habilidade from being saved:</h2>

      <ul>
      <% vaga_habilidade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :vaga_id %>
    <%= form.text_field :vaga_id %>
  </div>

  <div class="field">
    <%= form.label :habilidade_id %>
    <%= form.text_field :habilidade_id %>
  </div>

  <div class="field">
    <%= form.label :nivel %>
    <%= form.number_field :nivel %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
