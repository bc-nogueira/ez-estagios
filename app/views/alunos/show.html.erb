<div class="card p-3 mb-3">
  <div class="row">
    <div class="col-2">
    </div>

    <div class="col-10">
      <h1><%= @aluno.pessoa.nome %></h1>
      <div class="row">
        <strong class="col-2">E-mail:</strong>
        <%= @aluno.pessoa.account.email %>
      </div>

      <div class="row">
        <strong class="col-2">CPF:</strong>
        <%= @aluno.pessoa.cpf %>
      </div>

      <div class="row">
        <strong class="col-2">Matrícula:</strong>
        <%= @aluno.matricula %>
      </div>
    </div>
  </div>
</div>

<%= link_to 'Voltar', alunos_path, class: 'btn btn-secondary' %>
<% if current_account.is_aluno? && current_account.perfil.papel.id == @aluno.id %>
  <%= link_to 'Editar', edit_aluno_path, class: 'btn btn-outline-info' %>
<% end %>